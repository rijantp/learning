import { OppDirective } from './opp.directive';

describe('OppDirective', () => {
  it('should create an instance', () => {
    const directive = new OppDirective();
    expect(directive).toBeTruthy();
  });
});
